import {Link} from 'react-router-dom';

const NotFoundPage = () => {
    return (
        <div className="app__notFound">
            <p className="app__notFound-content">This page doesn't exist((</p>
        </div>

    );
};

export default NotFoundPage;